APP_NAME=Swap_Circle
APP_ENV=local
APP_KEY=base64:UOYlkZwp8xz7iIHVMQC8EuO8RG0YWAPtT2h1Tqt0LVM=
APP_DEBUG=true
APP_URL=http://127.0.0.1:8001
SIGNUP_CORPORATE_ENABLED=false
# Set to 0 on staging/local so claims can be tested immediately after payment.
# Omit or leave blank in production to use system_settings.claim_waiting_days (default 30).
CLAIM_WAITING_DAYS=0
ASSET_URL=http://127.0.0.1:8001
API_URL=http://127.0.0.1:8001/api/
WEB_URL=http://127.0.0.1:8001/

LOG_CHANNEL=stack

DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=bonsifynew
DB_USERNAME=root
DB_PASSWORD=ankitDev

BROADCAST_DRIVER=log
CACHE_DRIVER=file
QUEUE_CONNECTION=sync
SESSION_DRIVER=file
SESSION_LIFETIME=120
# On HTTPS staging/production:
# SESSION_SECURE_COOKIE=true

REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

# MAIL_MAILER=smtp
# MAIL_HOST=mail.swapcircle.trade
# MAIL_PORT=465
# MAIL_USERNAME=contact@swapcircle.trade
# MAIL_PASSWORD=contact@swapcircle.trade
# MAIL_ENCRYPTION=ssl

MAIL_MAILER=smtp
MAIL_SCHEME=null
MAIL_HOST=127.0.0.1
MAIL_PORT=25
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_FROM_ADDRESS="hello@example.com"
MAIL_FROM_NAME="${APP_NAME}"

MAIL_FROM_ADDRESS=contact@swapcircle.trade
MAIL_FROM_NAME="Swap Circle"

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=

PUSHER_APP_ID=
PUSHER_APP_KEY=
PUSHER_APP_SECRET=
PUSHER_APP_CLUSTER=mt1

BUSINESS_EMAIL=support@connevents.com
ONE_SIGNAL_APP_ID=
STRIPE_SECRET=
STRIPE_PUBLISHABLE=
STRIPE_WEBHOOK_SECRET=

# Hours after which unpaid Pending purchases auto-expire (scheduler: purchases:expire-pending)
PURCHASE_PENDING_EXPIRE_HOURS=24

# Stripe Dashboard webhook URL (staging/live):
#   POST {APP_URL}/api/stripe/webhook
# Events: checkout.session.completed, checkout.session.async_payment_succeeded


TIME_ZONE=Asia/Karachi

AGORA_APP_ID=
AGORA_APP_CERTIFICATE=

MIX_PUSHER_APP_KEY="${PUSHER_APP_KEY}"
MIX_PUSHER_APP_CLUSTER="${PUSHER_APP_CLUSTER}"